# DELIVERY — course-correction Phase E (2026-07-18)

Delivered from `tzervas/mycelium` branch `claude/mycelium-repos-alignment-1dohde` (post-AX-core
train, v0.464.0). Honesty ladder (VR-5): only the rows below are refreshed; every other file in
this repo (Grok-era seed drafts, `experiments/vet` artifacts) is untouched and remains `Declared`
— transpiler emissions stay `Declared` until a differential witnesses them (M-991 verdict; the
Phase-D readiness measurement is the monorepo's
`docs/planning/course-correction-2026-07-18/PHASE-D-READINESS.md`: all-7 pilot
`checked_fraction` 28.7%, zero P0 gap classes, ONESHOT one-shot claims HOLD).

## Refreshed files (graduated, hand-vetted)

| File | Provenance | Guarantee basis |
|---|---|---|
| `lib/compiler/ambient.myc` | refreshed from monorepo `lib/compiler/ambient.myc` | `Empirical` — myc-check-clean as part of the monorepo `lib/compiler` project; differential-witnessed by the self-hosted suites |
| `lib/compiler/ast.myc` | refreshed from monorepo `lib/compiler/ast.myc` | `Empirical` — myc-check-clean as part of the monorepo `lib/compiler` project; differential-witnessed by the self-hosted suites |
| `lib/compiler/lex.myc` | refreshed from monorepo `lib/compiler/lex.myc` | `Empirical` — myc-check-clean as part of the monorepo `lib/compiler` project; differential-witnessed by the self-hosted suites |
| `lib/compiler/nodule.myc` | refreshed from monorepo `lib/compiler/nodule.myc` | `Empirical` — myc-check-clean as part of the monorepo `lib/compiler` project; differential-witnessed by the self-hosted suites |
| `lib/compiler/parse.myc` | refreshed from monorepo `lib/compiler/parse.myc` | `Empirical` — myc-check-clean as part of the monorepo `lib/compiler` project; differential-witnessed by the self-hosted suites |
| `lib/compiler/semcore.myc` | refreshed from monorepo `lib/compiler/semcore.myc` | `Empirical` — myc-check-clean as part of the monorepo `lib/compiler` project; differential-witnessed by the self-hosted suites |
| `lib/compiler/substrate.myc` | refreshed from monorepo `lib/compiler/substrate.myc` | `Empirical` — myc-check-clean as part of the monorepo `lib/compiler` project; differential-witnessed by the self-hosted suites |
| `lib/compiler/token.myc` | refreshed from monorepo `lib/compiler/token.myc` | `Empirical` — myc-check-clean as part of the monorepo `lib/compiler` project; differential-witnessed by the self-hosted suites |
| `lib/compiler/totality.myc` | refreshed from monorepo `lib/compiler/totality.myc` | `Empirical` — myc-check-clean as part of the monorepo `lib/compiler` project; differential-witnessed by the self-hosted suites |

## This repo's `myc-check --project lib` result at delivery (no-op)

`CLEAN — ok: 9 file(s) checked, no findings`

A `FINDINGS` result here reflects the `Declared` seed-draft files, not the refreshed graduated
rows (those check clean in their monorepo project context). No tag is upgraded by this delivery.
